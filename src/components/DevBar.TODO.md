# DevBar.TODO.md

## Minor

- Stale comment: header says dev mode toggles "with 10 clicks on the Sun", but the actual
  gesture in Header.jsx fires at 5 clicks (`n >= 5`). Fix this comment to 5.

Otherwise OK. (FIXED: comment now says 5 clicks.)
